import React from "react";
import { apilocalUrl } from "../../config/settings";

const PaternSlaider = () => {
  const style = {
    width: "100%",
    heidth: "100%",
    backGround: `${apilocalUrl}/images/brands/serviciosSunat.png`,
  };
  return <div className="brand"></div>;
};

export default PaternSlaider;
